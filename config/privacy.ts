// Draft retention policy. NOT YET ENFORCED — no automatic deletion job
// exists in v0.5.4. Do not surface this number as an active guarantee in
// any UI copy until the deletion job in TODO(v0.6) ships; PrivacyDetails
// intentionally uses honest, non-committal wording instead.
export const MEDIA_RETENTION_DAYS = 7;
